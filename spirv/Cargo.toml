[package]
name = "spirv"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
quote = "1.0"
syn = { version = "2.0", features = ["visit"] }
naga = { version = "0.14", features = ["wgsl-in", "spv-out"] }
